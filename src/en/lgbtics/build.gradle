ext {
    extName = 'Lgbtics'
    extClass = '.Lgbtics'
    extVersionCode = 1
    isNsfw = true
}

apply plugin: "kei.plugins.extension.legacy"

android {
    defaultConfig {
        manifestPlaceholders += [
            SOURCEHOST: "lgbtics.com",
            SOURCESCHEME: "https"
        ]
    }
}

dependencies {
    implementation(project(":lib-multisrc:madara"))
}
